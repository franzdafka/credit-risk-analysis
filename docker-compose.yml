services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.app
    environment:
      CREDIT_API_URL: http://api:8000
    depends_on:
      - api
    ports:
      - "8501:8501"
